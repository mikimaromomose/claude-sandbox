Claude Code の操作感を試すための小さい検証用プロジェクトです。

Python で簡単な CLI を作ります。

## 使い方

```bash
python hello.py
```

引数を省略した場合は `Claude` が名前として使われます。

```bash
python hello.py World
```

名前を指定すると `Hello, World!` のように出力されます。
